name: Frontend CI

on:
  pull_request:
  push:
    branches:
      - main
      - pipeline-frontend # kan fjernes senere

jobs:
  statiske-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci
        working-directory: ./frontend/keramik-app

      - name: Run ESLint
        run: npm run lint
        working-directory: ./frontend/keramik-app

      - name: Run Prettier (check only)
        run: npm run format:check
        working-directory: ./frontend/keramik-app

  build-frontend:
    runs-on: ubuntu-latest
    needs: statiske-tests

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci
        working-directory: ./frontend/keramik-app

      - name: Build Next.js project
        run: npm run build
        working-directory: ./frontend/keramik-app
